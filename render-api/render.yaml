services:
  - type: web
    name: comfyui-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: MODAL_WEBHOOK_URL
        sync: false